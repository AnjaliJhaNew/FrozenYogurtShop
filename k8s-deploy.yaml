apiVersion: apps/v1
kind: Deployment
metadata:
  name: frozenyogurtshop-anjali
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frozenyogurtshop-anjali
  template:
    metadata:
      name: frozenyogurtshop-anjali
      labels:
        app: frozenyogurtshop-anjali
    spec:
      containers:
      - name: ovs
        image: jaibw/frozenyogurtshop:latest 
        ports:
        - containerPort: 80 

---
apiVersion: v1
kind: Service
metadata:
  name: frozenyogurtshop-anjali
spec:
  type: NodePort
  ports:
  - targetPort: 80 
    port: 80
  selector:
    app: frozenyogurtshop-anjali
